import { AuthPage } from "@/components/auth";

export const metadata = {
  title: "Auth | Jubayer Khan",
  description: "Secure login, signup, and Google OAuth with Better Auth.",
};

export default function Page() {
  return <AuthPage />;
}
