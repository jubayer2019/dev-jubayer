# Local dev can use the Express server directly. In production, set this to the deployed backend origin only, without a path.
NEXT_PUBLIC_API_URL=https://server-dev-jubayer.vercel.app/
NEXT_PUBLIC_AUTH_URL=http://localhost:5000/api/auth
NEXT_PUBLIC_APP_URL=http://localhost:3000
